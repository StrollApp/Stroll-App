import { DefaultTheme } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  roundness: 2,
  colors: {
    ...DefaultTheme.colors,
    primary: "#657cf5",
    accent: "#657cf5",
    blue: "#657cf5",
    black: "#1d1c1d",
    beige: "#c4b7ae",
    red: "#bb3543",
    green: "#518959"
  }
};

export default theme;
